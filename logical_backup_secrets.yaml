spec:
  kubernetes:
    services:
      cray-postgres-operator:
        postgres-operator:
          configLogicalBackup:
            logical_backup_s3_access_key_id:
            logical_backup_s3_secret_access_key:
