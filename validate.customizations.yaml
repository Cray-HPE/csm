# Overrides when validating helm charts that are only applicable during validation CI time.
cray-powerdns-manager:
  manager:
    base_domain: shasta.dev.cray.com
cray-dns-unbound:
  domain_name: shasta.dev.cray.com
